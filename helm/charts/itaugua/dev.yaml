app:
  namespace:
    enabled: true
    name: "itaugua"
    labels:
      owner: "devops"
      team: "plataform-devops"

  configMap:
    enabled: true
    name: "itaugua-config-map"
    annotations:
      description: "Configuration for the Itaugua service"
    data:
      NGINX_PORT: "8080"
      APP_ENV: "dev"

  service:
    name: "itaugua-service"
    type: ClusterIP
    port: 8080
    targetPort: 8080
    selector:
      app: "itaugua"
  
  secret:
    enabled: true
    name: "itaugua-secrets"
    annotations: 
      description: "Secrets for the Itaugua service"    
    labels:
      name: "itaugua-secrets"
    data:
       API_KEY: "ak_7FJ9dP2mQ4Xb81tRv6Kc3wLhZ0SgNEB"
       DB_PASSWORD: "jkl.123456*"
       
  deployment:
    name: "itaugua-deployment"
    replicas: 1
    minReadySeconds: 0
    revisionHistoryLimit: 3
    progressDeadlineSeconds: 600
    labels:
      app: "itaugua"
    enableStrategy: false
    enablePodSecurityContext: false
    serviceAccount: {enabled: false}
    initContainers: []
    podAnnotations: {}
    nodeSelector: {}
    container:
      name: "nginx-container"
      image: nginx:1.27-alpine
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 8080
      envFrom:
        - configMapRef:
            name: "itaugua-config-map"
        - secretRef:
            name: "itaugua-secrets"
      resources:
        limits:
          cpu: "500m"
          memory: "512Mi"
        requests:
          cpu: "200m"
          memory: "256Mi"
      enableStaticEnv: false
      enableVolumesMounts: false
      enableContainerSecurityContext: false
      env: []
      volumeMounts: []
  
  secretStore:
    enabled: false

  serviceAccount:
    enabled: false
  
  ingress:
    enabled: false